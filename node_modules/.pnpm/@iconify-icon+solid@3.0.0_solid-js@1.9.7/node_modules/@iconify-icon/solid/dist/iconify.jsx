/**
 * Export functions
 */
export { iconLoaded, getIcon, listIcons, addIcon, addCollection, calculateSize, buildIcon, loadIcons, loadIcon, addAPIProvider, setCustomIconLoader, setCustomIconsLoader, appendCustomStyle, _api, } from 'iconify-icon';
/**
 * Solid component
 */
export function Icon(props) {
    let { icon, mode, inline, rotate, flip, width, height, preserveAspectRatio, noobserver, } = props;
    // Convert icon to string
    if (typeof icon === 'object') {
        icon = JSON.stringify(icon);
    }
    return (
    // @ts-ignore
    <iconify-icon attr:icon={icon} attr:mode={mode} attr:inline={inline} attr:rotate={rotate} attr:flip={flip} attr:width={width} attr:height={height} attr:preserveAspectRatio={preserveAspectRatio} attr:noobserver={noobserver} {...props}/>);
}
